<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuanLiChiTieu"
        tools:targetApi="31">
        <activity android:name=".UI.NhomGiaoDich"
            android:exported="true" />

        <activity android:name=".UI.LoiNhacActivity"
            android:exported="true" />
        <activity android:name=".UI.TaiKhoanCuaToiActivity"
            android:exported="true" />
        <activity android:name=".UI.ThongBaoActivity"
            android:exported="true" />


        <!-- Các Activity phụ -->
        <activity android:name=".Chi_Tieu_Statistic" />
        <activity android:name=".Thu_Nhap_Statistic" />
        <activity android:name=".Group_Chi_statistic" />
        <activity android:name=".Group_Thu_statistic" />

        <!-- Activity chính (launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>